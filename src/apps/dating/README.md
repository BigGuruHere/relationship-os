# Dating application

The Dating application uses ContextSpaces with `domainKey = dating` and cannot fall back to Business custody.

Stage 8.9 contains only the boundary landing and controlled ContextSpace setup. Stage 8.10 will add the first single-sided voice-feedback workflow here.
